2) A pair of tupils as seen in the website states that, a pair of tuple will always generalize no matter what. meaning that "a tuple is a pair of generlization"
3) Tuples are a type of and exmaple of a pair. A tuple is a sequence (or ordered list) of elements.A tuple is defined inductively using the construction of an ordered pair.In computer science, tuples come in many forms.
